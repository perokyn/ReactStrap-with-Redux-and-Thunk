import { PROMOTIONS } from '../shared/promotions';//data source

export const Promotions = (state = PROMOTIONS, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
